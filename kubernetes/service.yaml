apiVersion: v1
kind: Service

metadata:
  name: security-runtime-api-service

spec:
  type: LoadBalancer

  selector:
    app: security-runtime-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      